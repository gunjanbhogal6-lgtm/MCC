version: '3.8'

services:
  autoseo-api:
    build: .
    ports:
      - "9100:9100"
    volumes:
      - ./data:/app/data
      - ./sites:/app/sites
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
